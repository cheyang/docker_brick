nc_server:
  build: ./nc_server
  ports:
    - "5432:8080"
  tty: true
  command: nc -l 8080
nc_client:
  image: rastasheep/ubuntu-sshd:14.04
  ports:
    - "3001:22"
  tty: true
  command: /usr/sbin/sshd -D
  links:
    - nc_server