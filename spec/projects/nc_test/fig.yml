ncserver:
  build: ./ncserver
  ports:
    - "5432:8080"
  tty: true
  command: nc -l 8080
ncclient:
  image: rastasheep/ubuntu-sshd:14.04
  ports:
    - "3001:22"
  tty: true
  command: /usr/sbin/sshd -D
  links:
    - ncserver